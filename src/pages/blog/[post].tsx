import { InternalLink } from "../../components/InternalLink";

// eslint-disable-next-line import/no-default-export
export default function Post(): JSX.Element {
  return <InternalLink href="/">index</InternalLink>;
}
